<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.summonsSystem.mapper.SensorEventLogMapper">

    <insert id="insert" parameterType="com.ruoyi.summonsSystem.vo.SensorEventLog" useGeneratedKeys="true" keyProperty="id">
        insert into ticket_sensor_event_log
        (rfid, ant, sensor_ip, sensor_port, event_type, remark, event_time, process_code, processed, process_result)
        values
        (#{rfid}, #{ant}, #{sensorIp}, #{sensorPort}, #{eventType}, #{remark}, #{eventTime}, #{processCode}, #{processed}, #{processResult})
    </insert>

    <update id="updateProcessed" parameterType="com.ruoyi.summonsSystem.vo.SensorEventLog">
        update ticket_sensor_event_log
        set processed = #{processed}, process_code = #{processCode}, process_result = #{processResult}
        where id = #{id}
    </update>

</mapper>
